<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="org.apache.commons.lang3.StringEscapeUtils" />
        <import type="android.text.Html" />
        <variable name="listener" type="de.fhg_radolfzell.android_app.main.feed.FeedAdapter" />
        <variable
            name="item"
            type="de.fhg_radolfzell.android_app.main.feed.FeedDataWrapperImpl.FeedItem" />
    </data>

    <android.support.v7.widget.CardView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/item_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:background="@color/blue_grey_50"
        android:elevation="12dp"
        android:orientation="vertical"
        app:cardCornerRadius="4dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onClickListener="@{(view) -> listener.onClick(item.link)}"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{StringEscapeUtils.unescapeHtml4(item.title)}"
                android:textAppearance="?android:attr/textAppearanceLarge"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@{Html.fromHtml(StringEscapeUtils.unescapeHtml4(item.content))}"
                android:textAppearance="?android:attr/textAppearanceSmall" />

            <TextView
                android:id="@+id/item_meta"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:layout_marginTop="8dp"
                android:text="@{StringEscapeUtils.unescapeHtml4(item.author)}"
                android:textAppearance="?android:attr/textAppearanceSmall" />

        </LinearLayout>
    </android.support.v7.widget.CardView>
</layout>