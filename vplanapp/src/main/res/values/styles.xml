<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryDark">@color/brand_primary_dark</item>
        <item name="colorAccent">@color/brand_accent_dark</item>
    </style>

    <style name="AppTheme.Dialog" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="colorAccent">@color/brand_accent_dark</item>
    </style>

    <style name="AppTheme.Toolbar" parent="AppTheme">
        <item name="android:textColorPrimary">@color/toolbar_textColorPrimary</item>
        <item name="android:textColorPrimaryInverse">@color/toolbar_textColorPrimaryInverse</item>
        <item name="android:actionMenuTextColor">@color/toolbar_actionMenuTextColor</item>
        <item name="actionMenuTextColor">@color/toolbar_actionMenuTextColor</item>
        <item name="android:textColorSecondary">@color/toolbar_textColorSecondary</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
