<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="obj"
            type="de.jbamberger.api.data.VPlanRow" />

        <variable
            name="listener"
            type="Object" />

    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/item_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:vplanBackground="@{obj}"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/vplan_first"
                android:layout_width="0px"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="start"
                android:textAppearance="?android:attr/textAppearanceMedium"
                app:html="@{obj.grade}"
                tools:text="grade" />

            <TextView
                android:id="@+id/vplan_second"
                android:layout_width="0px"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center_horizontal"
                android:textAppearance="?android:attr/textAppearanceMedium"
                app:html="@{obj.hour}"
                tools:text="hour" />


            <TextView
                android:id="@+id/vplan_third"
                android:layout_width="0px"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center_horizontal"
                android:textAppearance="?android:attr/textAppearanceMedium"
                app:html="@{obj.subject}"
                tools:text="subject" />

            <TextView
                android:id="@+id/vplan_fourth"
                android:layout_width="0px"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="end"
                android:textAppearance="?android:attr/textAppearanceMedium"
                android:textColor="@{obj.omitted ? @color/vplan_text_omitted : @color/vplan_text_normal}"
                app:html="@{obj.omitted ? @string/vplan_omitted : obj.room}"
                tools:text="room" />
        </LinearLayout>


        <TextView
            android:id="@+id/vplan_content"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:textAppearance="?android:attr/textAppearanceSmall"
            app:html="@{obj.content}"
            tools:text="content" />

    </LinearLayout>
</layout>